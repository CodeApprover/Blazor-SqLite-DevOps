---
name: "Staging Tests"  # code-staging

on:
  workflow_run:
    workflows: "Development Tests"
    types:
      - completed

jobs:
  deploy-to-staging:
    runs-on: ubuntu-latest
    steps:
    - name: Log Timestamp
      run: date
     
    - name: Checkout code
      uses: actions/checkout@v3.5.3

    - name: Download build artifact
      uses: actions/download-artifact@v2
      with:
        name: staging-artifact
        path: ./publish
